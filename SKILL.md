---
name: archival-ai
description: "[Process] 档案化：AI 语义翻译+脚本结构处理+安全网。触发：档案化/批量重命名/翻译文件名/去平台ID。"
license: Apache-2.0
version: 1.1.0
author: Xi
platforms: [windows, linux, macos]
metadata:
  hermes:
    tags: [process, archival, translation, rename]
    related_skills: [ai-workbench, verification-before-completion]
---

# Archival AI — 档案化 · 智能体优先版

> **定位：这是一个 Agent 能力（skill）——使用方是 AI 模型（agent），不是给用户手敲的 CLI。**
> agent 读本文档学习判断框架（A/B 模块、P1-P5、反模式），调用包内工具执行机械部分；
> 文档里的 CLI 命令是 agent 的内部执行通道，不是交付给用户的命令列表。
>
> **范式声明：AI 决定，用户确认，脚本执行，备份兜底。**
> 翻译是语义行为——理解文化、语境、中日英韩差异，这是 AI 的能力，不是映射表能替代的。
> 脚本的价值在执行与安全，不在决策。

## 这是什么 / 何时使用

**档案化** = 把从创作平台（fantia / fanbox / DLsite 等）下载的杂乱文件，整理成**规范、可检索、自包含**的命名：

```
原始（平台下载）:  2096f222_0zenra_24fps.mp4          ← hex hash + 罗马音 + 技术规格
清洗后（模块 A）:  231127_0zenra_24fps.mp4            ← 日期前缀 + 内容名 + 帧率
翻译后（模块 B）:  231127_全裸_24fps.mp4              ← AI 语义翻译（日→中）
```

**典型场景**：用户在 `C:\BaiduNetdiskDownload\...` 等下载目录积累了大量文件——文件名是平台生成的噪音（hash/ID/平台标记），无法检索、无法判断内容、重复文件难以发现。

**何时触发**（出现任一即使用本技能）：
- 用户说「档案化 / 整理这个目录 / 批量重命名」
- 文件名含平台噪音（hex hash、`(fantia500)`、`twitter_video_` 等）需要清理
- 文件名需要翻译（日/韩/英 → 中文）
- 需要给文件加日期前缀（按目录日期继承）或去重
- **面对新目录（第一次见该形态）** → 先 `skills/structure-scan/` 扫描判断能否处理

**领域背景（术语一次定义）**：
| 术语 | 含义 | 例子 |
|------|------|------|
| **平台噪音** | 下载来源平台生成的标识，无档案价值 | `2096f222_`（hex hash）、`twitter_video_`（平台前缀）、`(fantia500)`（档位） |
| **hex hash** | 8-32 位十六进制串（含 a-f），平台给文件的唯一 ID | `2096f222_0zenra` 中的 `2096f222_` |
| **档位标记** | 平台订阅档位（fantia 按 0/500/1000 日元分档） | 目录 `231127_1(0)`、`240116(500)` 的 `(0)`/`(500)` |
| **内容标记** | 档案核心信息，**必须保留**（不是噪音） | `[2D动画][有修正][无码][中字]` |
| **日期继承** | 文件无日期时，从目录层级继承日期作前缀 | 文件在 `2022-02-15-作品名` 目录 → 得 `220215_` 前缀 |

## 技能结构：两大模块

```
档案化
├── 模块 A：文件名清洗和规范化（结构 · 脚本机械）
│     A1 平台噪音删除 → A2 冗余删除 → A3 分隔符规范化
│     → A4 日期提取与统一 → A5 目录上下文继承 → A6 重名冲突处理
└── 模块 B：文件名翻译（语义 · AI 智能体）
      B1 语言与 token 识别 → B2 验证与查证 → B3 语义翻译
      → B4 基调与一致性 → B5 译名缓存维护
```

**子技能**：`skills/structure-scan/`（结构扫描与通用性增量）——每次面对**新目录**，
先扫描结构判断能否处理；不能 → 增量扩展技能（覆盖本次 + 保留之前）。
调用方式：读 `skills/structure-scan/SKILL.md` + `python skills/structure-scan/scripts/scan_structure.py <目录>`。
对照基准：`capability-matrix.md`（能力覆盖矩阵）。

### 模块 A：文件名清洗和规范化（脚本为主）

> **核心设计理念：逆向清除（v1/v2 传承）——"举证责任在删除方，不确定的就不动"**
> 1. **只删不增**：只删除确定无意义的内容（平台噪音/hex/副本/全角/零宽/连续分隔符）；清除后**全部保留**——保留是默认行为，不需要判断取舍（举证责任在删除方：删要理由，留不需要理由）
> 2. **不新增语义，只保留原有语义**：清除剥离噪音后，文件名**原有的语义自动显露、全部保留**——保留的内容本身就是语义（这正是清除的目的：删掉 `2096f222_` 后 `0zenra` 才真正可读）；作品名/标题等**目录层内容绝不复制进文件名**——那是新增语义、每次不同需逐条判断，违反逆向理念
> 3. 机械前缀仅两类：**日期**（A4 提取）与**无日期目录 context**（A5 结构继承）——都是固定规则，无需理解
> 4. 逆向规则固定、快速、可机械化；正向添加会越来越长且每次需要判断——不是本模块的设计

| 小模块 | 职责 | 执行者 | 示例 |
|--------|------|--------|------|
| A1 平台噪音删除 | 删平台ID（8位非日期数字）、hex hash、域名标签、平台名前缀、URL编码；**内容标记 `[2D动画][有修正]` 保留** | 脚本 | `2096f222_0zenra` → `0zenra` |
| A2 冗余删除 | 重复扩展名、下载副本后缀 `(1)`/`- Copy`/`副本` | 脚本 | `xxx(1).mp4` → `xxx.mp4` |
| A3 分隔符规范化 | 全角→半角、零宽删除、`・`→`_`、safe table、连续分隔符合并、首尾清理、分辨率标准化（`1280_720`→`720p`） | 脚本 | `２４fps` → `24fps` |
| A4 日期提取与统一 | 具体日期/日期范围统一识别（P1-P5 继承，见下方规则）；范围取起点 | 脚本 | `231127-260607` → `231127` |
| A5 目录上下文继承 | **全链继承**：父目录链（被执行目录下第一级 → 文件父目录）所有目录名，去日期/档位标记/伪扩展名后剩余作前缀；根→近顺序；越靠近根的目录被越多文件继承 | 脚本 | `2022-02-15-報いを受ける春ちゃん.psd` → `報いを受ける春ちゃん` |
| A6 重名冲突处理 | 重名自动 `_2` 后缀 | 脚本 | 同名 → `_2` |

- A 模块全部纯机械、确定性、幂等（重复运行结果不变）。
- 边界情况（不确定删不删）→ 标记留给 AI 判断，脚本不猜。

### 日期继承规则（A4，P1-P5 完备覆盖所有组合）

```
P1  文件自身有具体日期      → 用文件日期（最高：文件自己的信号最可信）
P2  文件自身有日期范围      → 用范围起点
P3  父目录链第一个具体日期  → 用它（具体优先于范围，跳过无日期目录）
P4  父目录链第一个日期范围  → 用范围起点
P5  全部无日期              → 不加前缀

排序原则: ① 文件信号 > 目录信号（靠近文件优先）
         ② 具体日期 > 日期范围（"范围里包含的具体日期来继承"）
排除: RJ 编号 / 平台标记(价格) / 歧义格式(MM-DD) 不识别为日期
幂等: 已带 YYMMDD 前缀的文件跳过，重复运行不变
D3 修正: 文件自带日期（素材日）≠ 目录日期（发布日）时 → 目录发布日优先作前缀，
         素材日期保留在原名（200609haru1 在 220215 目录 → 220215_200609haru1）
```

**日期格式**：YYMMDD（`231127`）/ YYYY-MM-DD（`2022-02-15`）/ 年月（`2025.09`→`2509`）/ 范围（`220215-260607`，取起点）。6 位日期后跟数字/字母也识别（`2203126kiri` → 素材日 220312 + 序号 6）。

**A5 全链继承（context）**：文件继承**所有父级目录名**（被执行目录下第一级 → 文件父目录）——越靠近根的目录被越多文件继承。每个目录名处理：去日期部分（日期已由 A4 前置，不重复）→ 去档位标记（`(fantia500)`/`(0)` 等）→ 去伪扩展名（目录名里的 `.psd`）→ 纯数字/符号剩余丢弃（档位目录 `0`/`500`）；有字母/日文才保留（`RJ01606066`、`scene01_LOOP`、标题 `報いを受ける春ちゃん`）。顺序根→近。

真实结构例：`作品名/220215-260607/231127-260607(fantia0)/231127_1(0)/xxx.mp4`
→ 4级 `231127_1(0)` 是具体日期 → 文件得 `231127_作品名_xxx.mp4`（P3；作品名全链继承，档位/期数纯数字丢弃）。
`2022-02-15-報いを受ける春ちゃん.psd/200609haru1.psd` → `220215_報いを受ける春ちゃん_200609haru1.psd`（标题继承）。
`RJ01606066/scene01_LOOP/ep01.mov` → `220215_RJ01606066_scene01_LOOP_ep01.mov`（RJ 编号是作品标识，进 context）。

### 模块 B：文件名翻译（AI 智能体为主）

| 小模块 | 职责 | 执行者 | 示例 |
|--------|------|--------|------|
| B1 语言与 token 识别 | 日/韩/英/中混合识别；token 分类（角色名/作品名/描述词/技术规格/类型标签/文化词/拟声词/屏蔽词） | AI | `Colored ver` = 描述词+技术规格 |
| B2 验证与查证 | **自主决定**是否搜索官方译名（web_search/Firecrawl）；无证据不翻 | AI + 搜索 | `セイバー` → 搜 → Saber |

**B2 高效策略——按作品批量提取多语言表**（用户方法论）：
1. **先判断作品归属**（B1 识别时判断：原神/碧蓝档案/hololive 等——认得出作品就搜作品）
2. **搜该作品的官方多语言角色名表**（官网/知名论坛/词典站——如原神 genshin-dictionary 日英中表、碧蓝档案 unisando 中文表记表）——**一次提取整表**，比逐角色搜高效且权威
3. **先验证表源有效**（确实提供该角色的日/英/中多语言译名——不是只有单一语言）再采用
4. 应用提取的译名（如 アイノ→爱诺、ヤフォダ→雅珂达、プルーネ→布伦妮——纠正音译猜测）
5. 作品名翻译（用户拍板）：翻译后无需可搜索（作者是检索锚点）；歌曲/有渠道平台才需要逆向搜索
| B3 语义翻译 | 语序重建（SOV→SVO）、の/ちゃん处理、Shinjitai→简体、文化语境理解 | AI | `帰宅中の桜ちゃん めがねなし` → `回家途中的小樱无眼镜` |
| B4 基调与一致性 | 先定调（成人/萌系/正经/技术）；跨文件统一译名、日英同名去重 | AI | 成人→直白词，萌系→软词 |
| B5 译名缓存维护 | 用户确认译名 → 缓存；只减搜索，不替代判断 | AI 记录 | `マヤ→玛雅` 入缓存 |

- B 模块每个决策都由 AI 理解做出。批量输入（一次读全部文件名）允许，放弃理解禁止。
- B2 搜索是 AI 的**自主判断工具**（需要时才搜），不是固定步骤，不强制每个 token 都搜。

### 模块 B 执行流程（翻译工作区——用户拍板设计）

**翻译 = 逐文件对照组**：原名/路径/译名三份文档同序一一对应（一起生成才对应）——
AI 拿到译名后按路径定位文件重命名（原名无路径无法定位）。回滚**通用**（备份与
模块 A 同格式，`--rollback` 按时间轴自动恢复——翻译无需单独回滚逻辑）。

```
① 生成工作区   python -m archival_pipeline.cli <目录> --gen-translation-list
               → <目录>\_translation\ 原名清单.txt（仅文件名无路径）
                 + 路径清单.txt（与原名清单同序一一对应——定位用）
② 手动翻译     基于原名清单逐条翻译 → 另存 译名清单.txt（同序）
③ 应用          python -m archival_pipeline.cli <目录> \
                   --apply-translation <译名清单.txt> --execute
               → 三件套按行配对（路径定位+原名校验）→ 重命名 + 自动备份
④ 精修         读对照组（原名/译名在一起）判断翻译问题 → 改译名清单 → 重跑 ③
               （初翻译靠理解可能有小问题——对照可发现——进入精细化语义优化）
```

**精修原则（用户方法论——以中国使用者体验为默认标准）**：
0. **默认标准 = 中国使用者的体验**：用户看到这个文件名——读起来顺不顺、理解是否零成本、
   辨识度够不够、会不会别扭/陌生——一切精修判断以「中文使用者视角」为出发点
   （`sxx→性` 使用者看到单字"性"干瘪别扭 → 口语"做爱"；不是字典对不对，是体验好不好）
1. **必须对照原名+译名一起看**——不能只看译名猜（精修依据 = "原名语义 vs 译名表达"的差异）
2. **可确定的才改**：①翻译不忠实（错译/漏译）②以使用者体验为标准，中文别扭但原名有明确
   语义可更自然（如 sxx 单字"性"→ 口语"做爱"——按基调选词）
3. **无法确定就保持**：原名无场景信息（`Arona_Tsukue`=桌——不知上/下/边）、语序非错误
   （`Drunken_Diona` 直译"醉酒_迪奥娜"忠实）→ 默认不变，不猜不改
4. 精修 = 改译名清单 → 重跑 ③（对照组反馈循环）

**对照组反馈循环**：AI 初翻译（信息少、靠理解）→ 因原名/译名**放在一起**作对照 →
AI 能判断翻译是否有问题 → 精细化语义优化环节（改译名清单重应用）。重名文件由
路径清单精确区分；翻译目标重名 A6 兜底（ensure_unique `_2`）。

## 模块组合（默认流程）

| 用户需求 | 组合 | 执行 |
|---------|------|------|
| 完整档案化（默认） | **扫描 → A → B** | ① structure-scan（新目录先判断能否处理）② A 执行 ③ 翻译工作区 → 译名应用 |
| 只要结构处理 | A 模块 | `python -m archival_pipeline.cli <目录> --execute`（自动备份） |
| 只要翻译 | B 模块 | `--gen-translation-list` 生成原名/路径清单 → 手动翻译 → `--apply-translation` 应用 |

**顺序说明**：清洗在前——平台噪音先删，AI 翻译输入干净；日期/上下文继承后，日文目录名也一并翻译，上下文完整。

## 运行模式

| 模式 | 触发 | 模块 B 怎么执行 |
|------|------|----------------|
| **精品模式**（默认） | 默认；或说「精修」 | AI 逐条理解 + 自主搜索 |
| **快速模式** | 用户明确说「快速」「批量」 | 常用词缓存（仅已知词，B5 缓存） |

- 精品模式是技能的本体；快速模式是显式降级，不是默认。
- 不确定时走精品模式。档案化不可逆——值得每次认真。
- 模块 A 两种模式都是脚本，无差别。

## AI 判断框架（认知，不是规则表）

### 1. 什么是好的文件名（翻译目标）
- **自然**：读起来像中文原生文件名，不是机翻腔
- **可搜索**：译名能搜到对应角色/作品（所以角色名要官方译名）
- **一致**：同一角色/术语跨文件统一
- **简洁**：信息块 ≤ 5，不冗余（包括日英同名去重）
- **最重要放最前**：决定排序的信息在最前面

### 2. 什么该翻 / 不该翻（默认翻译，保留是例外——用户方法论）

**默认：每个 token 先理解语义 → 翻译成中文**。使用者是中文母语者，文件名是给他看的——
母语直读（`可莉_强奸`）< 二次解码（`klee_rape`）；中文搜索也更自然（搜"强奸"优于搜"rape"）。
**"保留"不是默认——是"翻译后就不是它了"的例外**（标识符/数值）。

| 类别 | 处理 | 为什么 |
|------|------|--------|
| 角色名/作品名/描述词 | **翻译** | 有语义；官方译名（查作品多语言表）或直译 |
| 类型标签（rape→强奸、sxx→性、Feet_Hole→足穴、Comp→合成） | **翻译** | 都有语义；中文使用者母语直读 |
| 罗马音/英文 token | **先搜再翻** | 认得出作品→查该作品多语言表；搜过无果→音译 |
| 标识符（RJ 编号/平台 ID/hex） | 保留 | 翻译后无法对应回原文——翻了就不是它了 |
| 纯数值规格（1080p/4K/60fps/ver） | 保留 | 数值是通用语言，无内容语义 |
| 少数中文社区通用缩写（VR/MMD/CG） | 保留 | 中文圈也用，翻译反而不认识 |
| 歌曲名（无官方中文） | 保留 | 唯一"可搜索"例外：翻了搜不到原曲 |
| 拟声词（じゅぽ→啾噗） | 意译保声 | 保持声音效果，不直译含义 |
| 屏蔽词（○×） | 上下文还原 | 需作品语境推断，翻不出就标记 |
| 制作标记/创作者代号 | 先音译 | 辨识度 > 可读性（但先尝试音译，不默认保留）|

**判定自问**：「翻译成中文后，它还是它吗？」——是 → 翻；不是（标识符/数值）→ 保留。
**保留的举证责任在保留方**：保留一个 token 必须说得出"为什么不能翻"（对应回原文/数值/通用缩写），
说不出的就是没理解——继续查或问用户。

### 3. 什么时候搜索（B2 自主判断，不是固定步骤）
- **需要搜**：角色名/作品名不确定官方译名、文化特定词、跨作品同名角色（如 Saber）
- **不需要搜**：常识级译名、已有证据的已知译名、纯描述词
- 找到证据后记录来源（URL），无证据的翻译标记「待验证」——**宁可不翻，不可瞎翻**
- **无证据 ≠ 保留**：看不懂/不确定的 token → **先搜（默认动作）**——认得出作品就查该作品
  多语言表；搜过仍无证据 → 音译 / 标记待验证 / 问用户——保留是最后手段

### 3.5 查证通道路由（web-tool-router 思想：按任务类型分诊，API 优先、浏览器兜底）

| 查证任务 | 通道 | 说明 |
|---------|------|------|
| 角色/作品名官方译名、文化词（主场景） | `Xi/web-tool-router` → **firecrawl**（search/scrape） | LLM 就绪 markdown，快；首选 |
| GitHub 上的项目/文件命名约定、作者仓库 | `Xi/web-tool-router` → **gh CLI** | 结构化、有认证、快 |
| 需登录/JS 渲染的站点（pixiv、fantia 等） | `Xi/web-tool-router` → **Chrome MCP** | 兜底：登录态/动态渲染，最后手段 |

> 路由原则：查证 = agent 自主判断（web-tool-router 是通道选择器，不是搜索触发器）；能 API 解决的不用浏览器。

### 4. 基调判断（先定调，再翻译——逐 token 按最贴切基调选词）

基调是**多维混合**的：一个文件可同时是「成人 + 萌系 + 技术」——翻译时每个 token
按最贴切的基调选词，不是整文件一个调子。基调决定用词的**口语化程度与具体度**。

| 内容 | 用词方向 | 选词例子（好 > 差）|
|------|---------|------------------|
| 成人行为（sxx/sex 类） | **口语双字**，避免单字抽象书面词 | 做爱 > 性（单字干瘪，辨识度低）|
| 强奸/侵犯（rape 类） | 直白准确，法律/日常通用词 | 强奸/强暴 > 侵（太抽象）|
| 身体部位 | 常用口语词，按内容风格 | 乳首 > 乳头（看内容基调定）|
| 成人形容（全裸/泳装等） | 直接具体 | 全裸 > 裸（完整更自然）|
| 萌系/可爱 | 软萌口吻（不强行加"小"）| 按昵称惯例：小可莉（若社区如此）|
| 技术词 | 中文术语优先，数值保留 | 插帧/合成 > 保留英文缩写 |
| 专名/角色名 | 官方译名 > 音译 > 直译 | 卡琪娜（官方）|

**基调判断实操**：
1. 先扫目录整体印象（成人/萌系/正经/技术混合度）——决定大方向
2. 逐 token 选词：**这个词中文使用者口里怎么说**（不是字典直译）
3. 自问：「用户看到这个词会别扭/陌生吗？」——会 → 换更自然的表达
4. **单字词慎用**（性/爱/操 单字在文件名里辨识度低、易误解）——优先双字/常用短语
5. 同义候选里选**最具体、最口语**的那个（sxx → 做爱，不选"性"）

基调错了，翻译全错。先扫描目录判断基调，再动手。

### 5. 多语言差异要点（理解，不是查表）
| 维度 | 要点 |
|------|------|
| 日→中 | SOV→SVO 语序重建；「の」不无脑替换成「的」；ちゃん/さん→酱/桑；Shinjitai→简体 |
| 英→中 | 语义转换而非逐词：Hu Tao 4K Colored ver → 胡桃_4K_着色版 |
| 韩→中 | 中文译名优先于罗马音 |
| 文化词 | 理解梗/语境再译，不直译 |

## 脚本安全网（命令）

技能根目录 = `C:\Users\Administrator\AppData\Local\hermes\skills\Xi\archival-ai\`，命令需在此目录下运行：

| 任务 | 命令 |
|------|------|
| 结构预览——人类对照（txt，相对路径源名→最终新名） | `python -m archival_pipeline.cli <目录> --preview`（无参默认生成到目标目录 `preview_<时间戳>.txt`） |
| 结构预览——AI 机器可读（json） | `python -m archival_pipeline.cli <目录> --preview pv.json --format json` |
| 结构执行（**自动备份**到平台临时目录） | `python -m archival_pipeline.cli <目录> --execute` |
| 回滚（自动找最近备份） | `python -m archival_pipeline.cli <目录> --rollback`（无参） |
| 回滚（指定备份文件） | `python -m archival_pipeline.cli <目录> --rollback <备份文件...>` |
| 快速模式翻译预览（B5 缓存） | `python -m archival_pipeline.cli <目录> --translate table --preview pv.json --format txt` |
| 生成翻译工作区（原名+路径清单，同序配对） | `python -m archival_pipeline.cli <目录> --gen-translation-list` |
| 翻译应用（手动译名清单 → 重命名+备份，回滚通用） | `python -m archival_pipeline.cli <目录> --apply-translation <译名清单.txt> [--preview\|--execute]` |
| 平铺（可选，需显式） | `python -m archival_pipeline.cli <目录> --flatten` |
| 残留检测（假名/汉字残留） | `python scripts/check_translation.py <目录>` |
| 自检（验证安全网） | `python scripts/selftest.py` |

> 备份自动落盘（f2 undo 思路）：每次 `--execute` 后 JSON 备份存平台临时目录；`--rollback` 无参自动找匹配 target 的最近备份。自定义备份前缀 `--backup <前缀>` 可选。

### 双形态预览（执行前必须生成，给用户判断）

| 形态 | 格式 | 给谁 | 内容 |
|------|------|------|------|
| **人类对照** | txt（默认） | 用户 | **相对被执行目录的完整路径**逐条对照（`源相对路径 → 目标相对路径`）+ 统计——保留全部层级（日期继承来源/档位/目录结构），去掉盘符冗余；人类看层级变化即可判断操作是否正确、是否符合需求 |
| **AI 机器可读** | json | agent 后续处理 | 完整结构数据（original_paths/new_paths/statistics/steps） |

> 两条预览的 new_paths 都是**最终名**（original → final 最终态对照，非中间态）——链式中间态只存在于执行内部，不对外报告。**全量显示**：每个文件都有交代（变更对照 + 无变化清单——无变化本身是信息，证明已符合需求而非被漏掉）。

> 精品模式（默认）的翻译判断（译什么/怎么翻）由 AI 逐条理解完成；脚本负责执行
> 与安全网（`--gen-translation-list` 生成工作区、`--apply-translation` 应用 + 备份）。

| 模块 | 职责 | 为什么保留 |
|------|------|-----------|
| `archival_pipeline/steps/step1_processor.py` | A1-A3（平台噪音/冗余/分隔符）+ A6（ensure_unique） | 纯结构，机械正确 |
| `archival_pipeline/steps/step2_inherit_prefix.py` | A4-A5（日期/上下文继承） | 纯结构，机械正确 |
| `archival_pipeline/steps/conflict_detector.py` | 执行前冲突检测（链式覆盖/大小写碰撞/非法字符），有 error 阻断 | 安全放在执行层（pipeline.run 内自动调用） |
| `archival_pipeline/backup.py` | 操作前 JSON 备份，`rollback_all` 一键回滚 | 安全放在执行层 |
| `archival_pipeline/pipeline.py` | 链式预览/执行 + 后置验证（输出质量门控：非空/无连续分隔/无首尾分隔/扩展名保留） | 安全网 + 质量保障 |
| `scripts/check_translation.py` | 假名/汉字残留检测 | 后置验证，不是前置替代 |
| `archival_pipeline/steps/step0_translator.py` | B5 缓存翻译（快速模式专用） | 快速模式引擎；精品模式不使用 |

**降级为缓存**：`archival_pipeline/translator/cache.py` 是「已确认译名缓存」（560 条清洗后的词条库）——只减少重复搜索，绝不替代 AI 判断。AI 永远是最终裁判。

## 反模式（AI 常犯的错误）

| 反模式 | 正确做法 |
|--------|---------|
| 机翻腔（逐词替换） | 整句/整块理解后重建 |
| 过度翻译（把类型标签也翻了） | 先分类，再决定翻不翻 |
| 基调错乱（成人内容用雅词） | 先定调 |
| 把能搜到的当猜的 | 无证据不翻，标记待验证 |
| 批量跳过理解（查缓存命中就过） | 缓存仅供参考，每次独立判断 |
| 为了流程完整而搜索 | 搜索是判断工具，不是步骤 |

## 质量评估（六维 + 盲评）

AI 翻译完成后，用六维评分（1-10）：正确性 / 自然度 / 一致性 / 可搜索性 / 安全性 / 基调匹配。

**盲评原则**：评估者 ≠ 翻译者。AI 自查只能查机械残留；质量判断由用户抽查或换模型评估（免费模型池），避免自我偏好偏差。

## 数据积累

- **译名缓存**（B5）：用户确认过的译名 → 记录（含证据 URL）。语义：**减少重复搜索**。每次使用仍独立判断，缓存只是参考。
- **经验条目**：每次任务的反模式/边界案例 → 追加到 knowledge-base。
- **质量档案**：历次六维评分存档，形成质量趋势。

## 待验证假设（共同未知 → 最小实验）

| 假设 | 实验 | 单一变量 | 成功信号 | 回收数据 |
|------|------|---------|---------|---------|
| H1: AI 逐条精修成本可接受 | 同一目录跑 精品 vs 快速 | 处理方式 | 精品耗时/token ≤ 2×快速 | 耗时、token、搜索次数 |
| H2: AI 精修质量显著更高 | 两种结果盲评六维 | 处理方式 | 精品均分 ≥ 快速 +2 分 | 六维评分、用户偏好 |
| H3: 快速场景脚本级可接受 | 快速模式处理一批 | 用户容忍度 | 用户对结果无投诉 | 用户反馈、返工率 |

## 四不原则

- 不自动执行（方案必须给你确认）
- 不跳过预览（变更清单必须给你看过）
- 不覆盖备份（每次操作独立备份）
- 不做你不确认的操作（预览阶段有否决权）
